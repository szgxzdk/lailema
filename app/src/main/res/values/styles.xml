<resources>

    <style name="CustomActionBarStyle" parent="Widget.AppCompat.Light.ActionBar">
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        -->
        <item name="colorPrimary">@color/colorLailema</item>
        <item name="colorPrimaryDark">@color/colorLailema</item>
        <item name="colorAccent">@color/colorLailema</item>
        <item name="actionBarStyle">@style/CustomActionBarStyle</item>
    </style>

</resources>
